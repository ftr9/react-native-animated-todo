import { StyleSheet } from 'react-native';

export const G_IconStyles = StyleSheet.create({
  roundedBorderIcon: {
    borderWidth: 0.5,
    padding: 15,
    borderColor: '#adb5bd',
    borderRadius: 100,
  },
});
